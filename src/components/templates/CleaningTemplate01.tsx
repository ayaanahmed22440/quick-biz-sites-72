/**
 * Kept as a compatibility entry point. All designs now render through the
 * shared engine in LocalBusinessTemplate.
 */
export {
  LocalBusinessTemplate,
  LocalBusinessTemplate as CleaningTemplate01,
  type TemplateBusiness,
  type TemplateService,
  type TemplateArea,
  type TemplateHour,
  type LeadSubmit,
} from "@/components/templates/LocalBusinessTemplate";
