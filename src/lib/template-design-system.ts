/** Shared visual rules for every approved WebWarheads website template. */
export const TEMPLATE_TYPOGRAPHY = {
  displayFamily: '"Poppins", Arial, sans-serif',
  bodyFamily: '"Work Sans", Arial, sans-serif',
} as const;
